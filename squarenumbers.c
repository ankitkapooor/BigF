Bigf main()
oof:
	f.int.number;
	f.int.i;
	f.int.flag;
	flag=0;
	
	fout('Enter number to be checked: ');
	fin(number);
	
	for(f.i = 0 till number, f+) do
		if(i*i == number) do
			flag=flag+1;
			break;
		fif
	ffor
	##output##
	if(flag!=0) do
		fout('Value ', number, ' is a perfect square');
	else
		fout('Value ', number, ' is not a perfect square');
	fif
endoof:
